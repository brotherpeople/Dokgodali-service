# CSS는 dokgodali-*.css에서 맞춘 "선택자 한 줄" 컴팩트 스타일을 유지한다.
# Prettier의 CSS 프린터는 이 스타일을 지원하지 않고 항상 선언을 여러 줄로 풀어써서 강제 제외한다.
*.css

# HTML은 파일마다 기존 들여쓰기 관례(2칸/4칸/없음)가 달라 Prettier가 한 번에 밀어버리면
# 대규모 diff가 생긴다. 정리하고 싶으면 파일 단위로 따로 요청할 것.
*.html
